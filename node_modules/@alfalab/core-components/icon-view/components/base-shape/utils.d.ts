type SizePathsMap = {
    [size: number]: {
        top: string;
        bottom: string;
        topBottom: string;
        none: string;
    };
};
type GetPathParams = {
    size: number;
    hasTopAddons: boolean;
    hasBottomAddons: boolean;
    pathsMap: SizePathsMap;
};
type PathsMap = {
    shape: SizePathsMap;
    border: SizePathsMap;
};
declare const getPath: ({ size, hasTopAddons, hasBottomAddons, pathsMap, }: GetPathParams) => string;
export { PathsMap, getPath };
