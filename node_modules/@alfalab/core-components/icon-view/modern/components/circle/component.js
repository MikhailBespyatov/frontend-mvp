import React, { forwardRef } from 'react';
import 'classnames';
import { pathsMap } from './paths.js';
import '../base-shape/utils.js';
import { BaseShape } from '../base-shape/component.js';

const Circle = forwardRef((props, ref) => (React.createElement(BaseShape, Object.assign({}, props, { pathsMap: pathsMap, ref: ref }))));

export { Circle };
