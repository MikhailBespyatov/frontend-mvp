import React, { forwardRef } from 'react';
import 'classnames';
import '../base-shape/utils.js';
import { BaseShape } from '../base-shape/component.js';
import { pathsMap } from './paths.js';

const SuperEllipse = forwardRef((props, ref) => (React.createElement(BaseShape, Object.assign({}, props, { pathsMap: pathsMap, ref: ref }))));

export { SuperEllipse };
