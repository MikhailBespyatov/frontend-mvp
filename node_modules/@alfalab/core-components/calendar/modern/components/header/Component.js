import React from 'react';
import cn from 'classnames';

var styles = {"header":"calendar__header_c6vpt","withShadow":"calendar__withShadow_c6vpt","monthOnly":"calendar__monthOnly_c6vpt"};
require('./index.css');

const Header = ({ view = 'full', withShadow, children }) => {
    return (React.createElement("div", { className: cn(styles.header, {
            [styles.monthOnly]: view === 'month-only',
            [styles.withShadow]: withShadow,
        }), "aria-live": 'polite' }, children));
};

export { Header };
