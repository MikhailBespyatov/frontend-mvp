import { _ as __assign } from '../../tslib.es6-b934f8ee.js';
import React, { useCallback } from 'react';
import 'classnames';
import { isSameMonth, isThisMonth } from 'date-fns';
import '../../../../button/esm';
import { monthName } from '../../utils.js';
import { SelectButton } from '../select-button/Component.js';

var styles = {"monthsTable":"calendar__monthsTable_nb5el","button":"calendar__button_nb5el"};
require('./index.css');

var MonthsTable = function (_a) {
    var selectedMonth = _a.selectedMonth, _b = _a.months, months = _b === void 0 ? [] : _b, getMonthProps = _a.getMonthProps;
    var view = useCallback(function (month) {
        if (selectedMonth && isSameMonth(selectedMonth, month.date))
            return 'selected';
        if (isThisMonth(month.date))
            return 'outlined';
        return 'default';
    }, [selectedMonth]);
    return (React.createElement("div", { className: styles.monthsTable }, months.map(function (month) { return (React.createElement(SelectButton, __assign({}, getMonthProps(month), { key: month.date.getTime(), className: styles.button, view: view(month) }), monthName(month.date))); })));
};

export { MonthsTable };
