import React from 'react';
import cn from 'classnames';

var styles = {"component":"tabs__component_dfx5i","hidden":"tabs__hidden_dfx5i"};
require('./index.css');

const Tab = ({ children, hidden, className, disabled }) => children ? (React.createElement("div", { className: cn(styles.component, {
        [styles.hidden]: hidden,
    }, className), hidden: hidden, role: 'tabpanel', tabIndex: disabled ? -1 : 0 }, children)) : null;

export { Tab };
