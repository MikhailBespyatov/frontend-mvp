import React, { useContext, useEffect } from 'react';
import cn from 'classnames';
import '../../../../base-modal/modern';
import { ModalContext } from '../../Context.js';
import '../../../../button/modern';
import { Closer } from '../closer/Component.js';

const Header = ({ className, children, title, hasCloser = true, sticky, styles, }) => {
    const { headerHighlighted, setHasHeader } = useContext(ModalContext);
    const hasContent = title || Boolean(children);
    useEffect(() => {
        setHasHeader(true);
    }, [setHasHeader]);
    return (React.createElement("div", { className: cn(styles.header, className, {
            [styles.highlighted]: sticky && headerHighlighted,
            [styles.sticky]: sticky,
        }) },
        hasContent && (React.createElement("div", { className: styles.content },
            children,
            title && React.createElement("div", { className: styles.title }, title))),
        hasCloser && React.createElement(Closer, null)));
};

export { Header };
