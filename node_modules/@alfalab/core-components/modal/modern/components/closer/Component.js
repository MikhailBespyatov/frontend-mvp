import React, { useContext, useCallback } from 'react';
import cn from 'classnames';
import '../../../../base-modal/modern';
import { ModalContext } from '../../Context.js';
import { Button } from '../../../../button/modern';

var styles = {"closer":"modal__closer_1sswy","left":"modal__left_1sswy","right":"modal__right_1sswy","fullscreen":"modal__fullscreen_1sswy"};
require('./index.css');

const Closer = ({ className, fullscreen, align, ...restProps }) => {
    const { onClose } = useContext(ModalContext);
    const handleClick = useCallback(event => {
        onClose(event, 'closerClick');
    }, [onClose]);
    return (React.createElement(Button, Object.assign({ type: 'button', view: 'ghost', className: cn(styles.closer, className, fullscreen && styles.fullscreen, align && styles[align]), "aria-label": '\u0437\u0430\u043A\u0440\u044B\u0442\u044C', onClick: handleClick }, restProps)));
};

export { Closer };
