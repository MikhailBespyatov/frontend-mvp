import React from 'react';
import cn from 'classnames';
import { formatAmount, AMOUNT_MAJOR_MINOR_PARTS_SEPARATOR, THINSP } from '@alfalab/utils';

var styles = {"component":"amount__component_wjhw7","minorPartAndCurrency":"amount__minorPartAndCurrency_wjhw7"};
require('./index.css');

/**
 * Компонент для отображения суммы, согласно следующему гайдлайну:
 * https://design.alfabank.ru/patterns/amount
 */
const Amount = ({ value, minority, currency, view = 'default', rightAddons, showPlus = false, className, dataTestId, }) => {
    const { majorPart, minorPart, currencySymbol } = formatAmount({
        value,
        currency,
        minority,
        view,
    });
    return (React.createElement("span", { className: cn(styles.component, className), "data-test-id": dataTestId },
        showPlus && value > 0 ? '+' : '',
        majorPart,
        React.createElement("span", { className: styles.minorPartAndCurrency },
            minorPart && AMOUNT_MAJOR_MINOR_PARTS_SEPARATOR,
            minorPart,
            currency ? `${THINSP}${currencySymbol}` : null,
            rightAddons)));
};

export { Amount };
