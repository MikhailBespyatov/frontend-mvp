import React, { memo } from 'react';
import cn from 'classnames';

var styles = {"bar":"stepped-progress-bar__bar_56afd","done":"stepped-progress-bar__done_56afd"};
require('./index.css');

var StepBar = memo(function (_a) {
    var isDone = _a.isDone;
    return (React.createElement("span", { "data-test-id": isDone ? 'on' : 'off', className: cn(styles.bar, isDone && styles.done) }));
});

export { StepBar };
