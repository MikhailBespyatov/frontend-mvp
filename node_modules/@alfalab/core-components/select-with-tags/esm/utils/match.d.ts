import { GroupShape, OptionShape } from "../../../select";
import { OptionMatcher } from "../types";
declare const filterOptions: (options: (OptionShape | GroupShape)[], inputValue: string, math?: OptionMatcher) => OptionShape[] | GroupShape[];
export { filterOptions };
